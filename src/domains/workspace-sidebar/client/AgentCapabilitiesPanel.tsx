/**
 * Removed from the DSH workspace sidebar per requirement: Agent capabilities
 * panel is not ported. This module replaces the original via alias.
 */
export default function AgentCapabilitiesPanel(_props: any): null {
  return null
}
